name: Daily Newspaper Scraper

on:
  schedule:
    # Runs at 17 minutes past the hour from 00:00 to 06:00 UTC every day
    # We use an odd minute (17) because GitHub Actions often drops top-of-the-hour (0) crons due to high global load.
    - cron: '17 0-6 * * *'
  workflow_dispatch: # Allows manual triggering from the GitHub Actions tab

jobs:
  scrape-and-deliver:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Scraper and Delivery Engine
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        STORAGE_CHANNEL_ID: ${{ secrets.STORAGE_CHANNEL_ID }}
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
      run: |
        python run_scrapers.py
